const expect = require("chai").expect;
process.env.NODE_ENV = "test";
const improv = require("../functions/improvement");

describe("improvement()", () => {
    beforeEach(() => {
        var players = ['A', 'B', 'C', 'D']
    })
    it("should properly calculate an improvement rate", () => {
    })
});